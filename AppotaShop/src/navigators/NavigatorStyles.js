import { StyleSheet } from "react-native";

export const navigatorStyles = StyleSheet.create({
  iconStyle: {
    fontSize: 30,
    color: "#26A69A"
  }
});

export const navigatorColor = {
  activeTintColor: "#1976d2",
  inactiveTintColor: "#90caf9",
  tabBackgroundColor: "#FFFFFF"
};

export const navigatorSize = {
  focusedSize: 30,
  normalSize: 20
};
